/* eslint-disable no-unused-vars */
localStorage.currentCounter = 0;

function getdetails(obj) {
  localStorage.allCounter = obj.dataset.item;
}

function getdetails1(obj) {
  localStorage.levelForAudition = obj.dataset.item;
}
